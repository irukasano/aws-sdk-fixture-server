## HLD

### 2026-09-14 15:00:35 JST : Issue #7 README の利用方法と対応範囲

- 目的: GitHub Issue #7 に従い、初見の利用者が README だけで本サーバーの位置付け、対応範囲、制約、ローカル起動、Scenario の読込、JavaScript / TypeScript または Python SDK テストの実行まで判断・実行できるようにする。
- 変更対象: `README.md` のみ。現在の実装・SDK テスト・Docker / Makefile と整合する説明、JavaScript / TypeScript と Python の最短利用例、Scenario・Control API・Cognito JWT/JWKS の利用説明を追加・更新する。
- 非変更対象: サーバー実装、SDK helper API、Scenario schema、Docker / Compose / Makefile、テスト、依存関係、対応 operation の追加・変更。
- 入出力: 入力は Issue #7 の記載、およびリポジトリにある現在の実装・SDK テスト・設定ファイル。出力は日本語の README とし、コマンド・HTTP endpoint・YAML・SDK コード例を含める。
- 運用方法: README の手順どおりに Docker Compose で起動し、FixtureSession を用いて session 作成、Scenario 読込、AWS SDK 呼出、request history の確認、session 破棄を行う。対応外または未定義の request は fail-fast になることを明記する。
- 失敗時挙動: 記述対象の挙動が実装・テストで裏付けられない場合、README に推測を記載せず、根拠を確認してから記述する。README の例を検証できない場合は実装を完了しない。
- 既存機能への影響: 実行時挙動は変えない。README を利用する人の理解と導入手順だけを更新する。
- 未確定事項: README の記載言語・具体的な章立て・サンプルの粒度は Issue #7 が限定しておらず、既存 README と実装を確認後に確認が必要。
- ユーザー確認が必要な項目: この HLD（README のみを変更し、現行実装を正とすること）の承認。承認後に、実装実例に合わせた章立てと検証計画を `## Plan` に記録して承認を得る。

## Plan

### 2026-09-14 15:02:39 JST : Issue #7 README の利用方法と対応範囲

- [x] ユーザー指示により、`implementation-workflow` の RED テスト追加・独立 RED テストレビューは本ドキュメント変更では実施しない。この例外以外のスコープ制約と最終検証・コミット手順は守る。
- [x] `README.md` に、決定的な AWS SDK 互換 fixture server としての位置付け、非エミュレータであること、対応外 / 未定義 request の fail-fast、非目標と制約（実 AWS・サービス状態・IAM・Hosted UI を扱わないこと、同一 process で FixtureSession を並行利用できないこと、endpoint を client 単位で明示指定する場合の制約）を記載する。
- [x] 現行の `defaults/scenario/` と request 正規化に基づき、Secrets Manager `GetSecretValue`、S3 `GetObject` / `PutObject` / `HeadObject`、SQS `SendMessage`、Bedrock Runtime `InvokeModel` / `Converse`、Cognito IDP の6 operation と、Cognito の session 固有 OIDC discovery / JWKS / RS256 署名 JWT の対応範囲を一覧化する。
- [x] Docker / Docker Compose の前提、`docker compose up --build`、port `4566`、`/__fixture/health`、テスト用 AWS 環境変数、`make go-test` / `make sdk-test` / `make test` を、Makefile と Compose 定義どおりに記載する。
- [x] JavaScript / TypeScript と Python について、FixtureSession の開始、Scenario 読込、通常生成した SDK client による呼出、request history、破棄を示す最短例を記載する。helper が process の `AWS_ENDPOINT_URL`（および service 固有 endpoint 変数）を session endpoint に設定し、破棄時に復元することを明記する。
- [x] Scenario の `service`、`operation`、`match`、`response`、`error`、`sequence`、`use_defaults`、同梱 default Scenario と project-owned Scenario の関係、成功・エラー・sequence の最小 YAML 例、Control API の endpoint・HTTP method・用途を現実装に合わせて記載する。
- [x] Cognito での `AuthenticationResult` JWT template、session issuer と discovery / JWKS URL、予約 claim の server 注入、ユーザー・認証・OTP 状態を保持しない制約を記載する。
- [x] `make test` を実行して既存の Go / JavaScript / Python の互換テストを確認し、README のコマンド・対応 operation・利用例・制約と差分を突き合わせる。`git diff --check` と HLD / Plan scope review を行う。
- [x] `commit-workflow` に従い、承認済み範囲の `README.md` と本セッション記録だけを stage し、Issue #7 を付した commit を作成する。push はしない。

## Review

### 2026-09-14 15:14:08 JST : Issue #7 README の利用方法と対応範囲

- 原因: README が一行のみで、Issue #7 が求める利用手順、対応範囲、Scenario、Control API、Cognito の境界を利用者が判断できなかった。
- 修正内容: README に対応 operation、非目標と endpoint 環境変数の制約、Docker / Compose の起動・healthcheck・Make コマンド、JavaScript / Python `FixtureSession` の最短例、Scenario と Control API、Cognito JWT / issuer / JWKS を追加した。最終レビューで、実装にない healthcheck 待機の断定を削除し、到達不能だった sequence YAML 例を相互排他的な match に修正した。
- 検証結果: `make test` の Go 部分は成功（`cmd/healthcheck`、`src/server`）。Docker Buildx の sandbox 外 state が必要だったため、build 済みの healthy `aws-fixture` に対して `docker compose run --rm --no-deps javascript-sdk-tests`（1 passed）と `docker compose run --rm --no-deps python-sdk-tests`（1 passed）を実行した。`git diff --check` は成功し、Docker Compose cleanup 後の差分は README とこのセッション記録だけである。
